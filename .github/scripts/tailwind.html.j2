<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>nyxtudes</title>
  <!-- Include Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-sans text-gray-800 bg-white p-5">    
  <div class="max-w-4xl mx-auto">
    <!-- Header -->
    <header class="bg-gray-100 p-6 text-center rounded-lg mb-6">
      <img src="https://avatars.githubusercontent.com/u/219992970" alt="nyx-4's logo" class="w-20 h-auto mx-auto mb-3">
      <h1 class="text-2xl font-bold mb-2">nyx-4/nyxtudes: marimo notebooks + manim animations</h1>
      <p class="text-sm">Python programs, usually short, of considerable difficulty, to perfect particular skills.</p>
    </header>

    <main>
      {% if m_marimo %}
      <h1 class="text-xl font-bold my-4">Marimo:</h1>
      <p>Reactive notebook experience for Python</p>
        {% for key, notebooks in m_marimo.items() %}
        <h2 class="text-xl font-bold text-center my-4">{{ key }}</h2>
        <p class="text-center mb-4">{{ description[key]['description'] }}</p>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
          {% for notebook in notebooks %}
          <!-- {{ notebook.name }} Notebook -->
          <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden">
          <div class="bg-gray-200 p-3 font-semibold">{{ notebook.name }}</div>
          <img src="{{ notebook.thumbnail }}" alt="thumbnail for {{ notebook.name }}">
          <div class="p-4">
            <a href="{{ notebook.mode1 }}" class="inline-block bg-amber-500 hover:bg-amber-600 text-white py-1 px-3 rounded transition-colors">Open App</a>
            <a href="{{ notebook.mode2 }}" class="inline-block bg-blue-500 hover:bg-blue-600 text-white py-1 px-3 rounded transition-colors">Open Notebook</a>
          </div>
          </div>
          {% endfor %}
        </div>
        {% endfor %}
      {% endif %}

      {% if m_manim %}
      <h1 class="text-xl font-bold my-4">Manim:</h1>
      <p>Animation engine for explanatory math videos</p>
        {% for key, manims in m_manim.items() %}
        <h2 class="text-xl font-bold text-center my-4">{{ key }}</h2>
        <p class="text-center mb-4">{{ description[key]['description'] }}</p>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
          {% for manim in manims %}
          <!-- {{ manim.name }} Animations -->
          <div class="bg-gray-50 border border-gray-200 rounded-lg overflow-hidden">
          <div class="bg-gray-200 p-3 font-semibold">{{ manim.name }}</div>
          <img src="{{ manim.thumbnail }}" alt="thumbnail for {{ manim.name }}">
          <div class="p-4">
            <a href="{{ manim.mode1 }}" class="inline-block bg-amber-500 hover:bg-amber-600 text-white py-1 px-3 rounded transition-colors">Light theme</a>
            <a href="{{ manim.mode2 }}" class="inline-block bg-blue-500 hover:bg-blue-600 text-white py-1 px-3 rounded transition-colors">Dark theme</a>
          </div>
          </div>
          {% endfor %}
        </div>
        {% endfor %}
      {% endif %}
    </main>

    <footer class="mt-10 pt-6 border-t border-gray-200 text-center text-sm text-gray-600">
      <p class="mb-2">Built with <a href="https://marimo.io" target="_blank" class="text-blue-500 hover:underline">marimo</a> ( Interactive Python notebooks ) and <a href="https://manim.community" target="_blank" class="text-blue-500 hover:underline">manim</a> ( Python library for creating mathematical animations )</p>
    </footer>
  </div>
</body>
</html>
